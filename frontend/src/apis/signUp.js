import { useSelector } from "react-redux"

//Signup API logic 
const signUp = (userInfo) =>{
    // useInfo를 파라미터로 받아서 검증 후 post요청
    if(1){ //검증 로직
        
    }
    else{
        //error 시 global warning 생성
    }
}

export { signUp }